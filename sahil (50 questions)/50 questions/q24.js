var arr=["a","b","c","d","1"]
for( var i=0;i<arr;i++)
{
     if(arr<=a || arr>=z)
     {
        console.log(`it is alphabet` + arr)
     }
     else{
         console.log(`it is a number` + arr)
     }
}

